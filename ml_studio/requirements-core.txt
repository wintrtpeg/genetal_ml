# 이것만 있으면 전 단계가 돕니다.
# 부스팅 3종과 SHAP 은 requirements-extra.txt 로 분리했습니다 —
# 그쪽 설치가 실패해도 도구는 sklearn 만으로 끝까지 동작해야 하기 때문입니다.

streamlit>=1.36
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
plotly>=5.20
joblib>=1.3
PyYAML>=6.0

# SQL 데이터마트 조회
#
# 1.4 도 지원합니다 — SQream 드라이버(pysqream-sqlalchemy)가 1.4 를 요구해서
# 함께 깔면 SQLAlchemy 가 1.4 로 내려갑니다. 이 도구가 쓰는 것은
# create_engine · engine.connect() · sa.text() · pd.read_sql 뿐이고
# 넷 다 1.4 와 2.0 에서 동작이 같습니다. 그래서 하한을 1.4 로 둡니다.
SQLAlchemy>=1.4
# SQream 드라이버는 requirements-extra.txt 에 있습니다 (run.bat 이 자동 설치).
# 다른 DBMS 를 쓰시면 아래에서 해당하는 것만 골라 설치하세요.
# oracledb           # Oracle
# psycopg2-binary    # PostgreSQL
# pymysql            # MySQL / MariaDB
# pyodbc             # SQL Server
# trino[sqlalchemy]  # Trino / Presto

# 테스트
pytest>=7.4
