# 있으면 좋지만 없어도 되는 것들.
#
# 부스팅 3종은 설치돼 있는 것만 모델 목록에 자동으로 잡힙니다 (core/models.py).
# SHAP 이 없으면 해석 화면이 순열 중요도로 대체됩니다.
#
# 이 파일 설치가 실패해도 도구는 그대로 돕니다. 특히 catboost 는 용량이 크고
# lightgbm 은 윈도우에서 재배포 패키지를 요구하는 경우가 있어 따로 뺐습니다.
#
#   pip install -r requirements-extra.txt
#
# 하나씩 넣어도 됩니다. 실패한 것만 빼고 나머지는 그대로 쓰입니다.

xgboost>=2.0
lightgbm>=4.0
catboost>=1.2
shap>=0.44

# SQream 데이터마트 접속 드라이버.
# 예전에는 "쓰는 DBMS 것만 골라 깔라" 며 빼 뒀는데, 정작 이 도구를 쓰는 곳이
# SQream 이라 환경 점검이 매번 "건너뜀 1" 로 끝났습니다. 이제 자동으로 넣습니다.
# 설치에 실패해도 나머지는 그대로 쓰이고, SQL 화면만 못 씁니다.
pysqream-sqlalchemy
